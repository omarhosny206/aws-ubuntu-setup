[user]
        name = omarhosny206
        email = omarhosny102@gmail.com
[credential]
        helper = store
[alias]
lg = !git --no-pager log --graph --abbrev-commit --decorate --format=format:'%C(bold blue)%h%C(reset) - %C(bold green)(%ar)%C(reset) %C(white)%s%C(reset) %C(dim white)- %an%C(reset)%C(auto)%d%C(reset)' --all && echo
